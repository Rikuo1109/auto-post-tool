name: Django CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.7, 3.8, 3.9]

    steps:
    - uses: actions/checkout@v3
    - name: Push noti
      run: |
        curl -X POST --data-urlencode "payload={\"channel\": \"#intern-webhook\", \"username\": \"Death-bot\", \"text\": \"Lo\"}" https://hooks.slack.com/services/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
        
